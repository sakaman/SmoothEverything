<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SmoothEverything.Settings.Pages.ApplicationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Required"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded"
    mc:Ignorable="d">

    <ScrollViewer
        HorizontalContentAlignment="Stretch"
        HorizontalScrollBarVisibility="Disabled"
        HorizontalScrollMode="Disabled">
        <Grid HorizontalAlignment="Stretch">
        <StackPanel MaxWidth="980" Margin="32,28,32,48" Spacing="22" HorizontalAlignment="Stretch">
            <StackPanel Spacing="6">
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="Application Rules" />
            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Set compatibility behavior by executable name. Exclusions take precedence over profiles." />
            </StackPanel>

            <InfoBar x:Name="PageMessage" IsOpen="False" IsClosable="True" Severity="Informational" />

            <StackPanel Spacing="10">
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Excluded Applications" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="These applications receive unmodified wheel events, which suits games, Remote Desktop, and software with its own scrolling engine." TextWrapping="Wrap" />
                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel Spacing="14">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        <TextBox x:Name="ExcludedInput" PlaceholderText="For example, game.exe" KeyDown="ExcludedInput_KeyDown" />
                        <Button Grid.Column="1" Content="Browse..." Tag="excluded" Click="Browse_Click" />
                        <Button Grid.Column="2" Content="Add" Click="AddExcluded_Click" />
                        </Grid>
                        <ItemsControl x:Name="ExcludedItems" HorizontalContentAlignment="Stretch">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Padding="8,6" ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon Glyph="&#xE8D7;" FontSize="16" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="1" Text="{Binding}" FontFamily="Cascadia Mono, Consolas" VerticalAlignment="Center" />
                            <Button Grid.Column="2" Content="Remove" Click="RemoveExcluded_Click" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    <TextBlock x:Name="NoExclusionsText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="No applications are excluded." />
                    </StackPanel>
                </Border>
            </StackPanel>

            <StackPanel Spacing="10">
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Application Profiles" />
                <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Override global motion settings for one application or enable compatibility mode to pass input through." TextWrapping="Wrap" />
                <Border Style="{StaticResource SettingsCardStyle}">
                    <StackPanel Spacing="14">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                        <TextBox x:Name="ProfileInput" PlaceholderText="For example, chrome.exe" KeyDown="ProfileInput_KeyDown" />
                        <Button Grid.Column="1" Content="Browse..." Tag="profile" Click="Browse_Click" />
                        <Button Grid.Column="2" Content="Create Profile" Click="AddProfile_Click" />
                        </Grid>

                        <ItemsControl x:Name="ProfileItems" HorizontalContentAlignment="Stretch">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border
                                        Margin="0,4"
                                        Padding="16"
                                        Background="{ThemeResource LayerFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                        <StackPanel Spacing="14">
                                            <Grid ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal" Spacing="10">
                                                    <FontIcon Glyph="&#xE71D;" FontSize="16" VerticalAlignment="Center" />
                                                    <TextBlock Text="{Binding Executable}" FontFamily="Cascadia Mono, Consolas" FontWeight="SemiBold" VerticalAlignment="Center" />
                                                </StackPanel>
                                    <Button Grid.Column="1" Content="Remove" Click="RemoveProfile_Click" />
                                            </Grid>

                                            <Grid ColumnSpacing="28">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <ToggleSwitch
                                        Header="Enable smoothing"
                                                    IsOn="{Binding Enabled}"
                                                    Tag="enabled"
                                                    Toggled="ProfileToggle_Toggled" />
                                                <ToggleSwitch
                                                    Grid.Column="1"
                                        Header="Compatibility mode"
                                                    IsOn="{Binding CompatibilityMode}"
                                        OffContent="Use settings below"
                                        OnContent="Pass through"
                                                    Tag="compatibility"
                                                    Toggled="ProfileToggle_Toggled" />
                                            </Grid>

                                <Expander Header="Application Motion Settings">
                                                <Grid Padding="0,12,0,0" ColumnSpacing="16" RowSpacing="12">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto" />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                            <NumberBox Header="Distance multiplier" Minimum="0.1" Maximum="4" SmallChange="0.05" Value="{Binding Motion.DistanceScale}" Tag="distance" ValueChanged="ProfileNumberBox_ValueChanged" />
                                            <NumberBox Grid.Column="1" Header="Animation duration (ms)" Minimum="1" Maximum="2000" SmallChange="10" Value="{Binding Motion.AnimationTimeMs}" Tag="duration" ValueChanged="ProfileNumberBox_ValueChanged" />
                                            <NumberBox Grid.Column="2" Header="Maximum acceleration" Minimum="1" Maximum="20" SmallChange="0.5" Value="{Binding Motion.AccelerationMax}" Tag="acceleration" ValueChanged="ProfileNumberBox_ValueChanged" />
                                            <NumberBox Grid.Row="1" Header="Acceleration window (ms)" Minimum="0" Maximum="500" SmallChange="5" Value="{Binding Motion.AccelerationWindowMs}" Tag="window" ValueChanged="ProfileNumberBox_ValueChanged" />
                                            <NumberBox Grid.Row="1" Grid.Column="1" Header="Tail / head ratio" Minimum="0.1" Maximum="10" SmallChange="0.1" Value="{Binding Motion.TailToHeadRatio}" Tag="tail" ValueChanged="ProfileNumberBox_ValueChanged" />
                                            <ToggleSwitch Grid.Row="1" Grid.Column="2" Header="Easing" IsOn="{Binding Motion.EasingEnabled}" Tag="easing" Toggled="ProfileToggle_Toggled" />
                                                </Grid>
                                            </Expander>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    <TextBlock x:Name="NoProfilesText" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="No application profiles have been created." />
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
